# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts keep LF line endings even when checked out on Windows
# (a CRLF testhub.sh fails inside WSL with "bad interpreter")
*.sh text eol=lf
