from django.apps import AppConfig


class FreightConfig(AppConfig):
    """Acme Freight: the built-in demo site the sample tests drive."""
    name = "freight"
    verbose_name = "Acme Freight demo site"
